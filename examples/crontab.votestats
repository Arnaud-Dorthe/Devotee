
# Create the html and other statistics
08             0-23 * * *  ($BINDIR/dvt-cron && $BINDIR/vote.stats) >> $HOME/$TEMPDIR/out 2>&1
18,28,38,48,58 0-23 * * *  ${BINDIR}/vote.stats >> $HOME/$TEMPDIR/out 2>&1

